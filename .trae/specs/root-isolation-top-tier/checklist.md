# 顶级零后门隔离式 Root 检测全域架构 - 检查清单

## 一、域 D 底层裸系统汇编抽象层检查

- [ ] `syscall_arm64.S` 纯汇编裸系统调用封装实现正确（SYS_openat、SYS_getdents64、SYS_truncate、SYS_read、SYS_write、SYS_mmap 等）
- [ ] `cpu_control.c` CPU 核心绑定（sched_setaffinity）功能实现正确
- [ ] `cpu_control.c` 调频锁定功能实现正确
- [ ] `cpu_control.c` 纳秒级高精度时钟（CLOCK_MONOTONIC_RAW）实现正确
- [ ] `raw_file_io.c` 原始 mmap 功能实现正确
- [ ] `raw_file_io.c` 无缓存裸读写文件功能实现正确
- [ ] `raw_file_io.c` 节点解析原语实现正确
- [ ] `memory_raw.c` 进程内存裸读写功能实现正确
- [ ] `memory_raw.c` 匿名内存分配功能实现正确
- [ ] `memory_raw.c` 地址随机化工具实现正确
- [ ] `crypto_basic.c` 底层 SHA3-512 哈希实现正确（自研轻量化，无第三方库）
- [ ] `crypto_basic.c` AES-256-GCM 加密工具实现正确（自研轻量化，无第三方库）
- [ ] 单元测试验证所有汇编 syscall 接口正确性通过

## 二、加密 IPC 通信层检查

- [ ] `unix_socket_server.c` Unix Socket 服务端（沙箱端）实现正确
- [ ] `unix_socket_server.c` Unix Socket 服务端（守护进程端）实现正确
- [ ] `unix_socket_client.c` Unix Socket 客户端（UI 端）实现正确
- [ ] AES-GCM 加密解密模块实现正确，支持消息认证码
- [ ] 64bit 递增序列号防重放机制实现正确
- [ ] 设备硬件随机盐值时间戳生成与校验实现正确
- [ ] 消息完整性校验实现正确，篡改直接丢弃并触发告警
- [ ] 单元测试验证加密通信安全性通过

## 三、域 B 沙箱微内核调度器核心检查

- [ ] `micro_kernel.c` 插件动态加载器实现正确（dlopen/dlsym 封装）
- [ ] `micro_kernel.c` 插件按需加载/卸载功能实现正确
- [ ] 动态乱序调度器实现正确（Fisher-Yates 洗牌算法随机打乱 7 层执行顺序）
- [ ] 纳秒级随机休眠噪声插入实现正确（100~5000ns 随机）
- [ ] CPU 绑定单元实现正确（每次检测随机绑定 1~2 个物理 CPU 核心）
- [ ] 内存隔离分配器实现正确（每层插件分配独立匿名私有内存缓冲区）
- [ ] 插件接口标准定义（plugin_interface.h）实现正确
- [ ] 单元测试验证微内核调度器功能通过

## 四、Plugin L1 原始属性裸读插件检查

- [ ] mmap `/dev/__properties__` 二进制解析实现正确（绕过 getprop）
- [ ] 裸 openat 遍历全量隐藏 su/ksu/adb 路径实现正确（100+ 变种）
- [ ] 属性关键词匹配实现正确（magisk、zygisk、su、ksu、apatch 等）
- [ ] 属性篡改标记与加密输出实现正确
- [ ] 编译为独立 SO 插件，与微内核接口兼容

## 五、Plugin L2 ART 虚拟机注入检测插件检查

- [ ] 裸读取 `/proc/self/maps` 实现正确
- [ ] 扫描匿名可执行内存实现正确
- [ ] libart.so 函数段本地哈希比对实现正确（检测 Inline Hook）
- [ ] 异常 ClassLoader 检测实现正确
- [ ] `/data/adb` dex 映射识别实现正确
- [ ] 编译为独立 SO 插件，与微内核接口兼容

## 六、Plugin L3 进程 & 内存指纹扫描插件检查

- [ ] 纯 syscall `getdents64` 遍历 `/proc` 实现正确（绕过进程隐藏）
- [ ] `/proc/self/mem` 逐段匹配 Rootkit 内存特征实现正确
- [ ] `memfd` jit 可执行匿名区域检测实现正确（Zygisk 模块识别）
- [ ] 内存特征库建立正确（libmagisk.so、libzygisk.so、liblspd.so 特征）
- [ ] 编译为独立 SO 插件，与微内核接口兼容

## 七、Plugin L4 命名空间挂载对比插件检查

- [ ] 沙箱内 fork 子线程创建独立 mnt 命名空间实现正确（clone CLONE_NEWNS）
- [ ] 父子进程并行读取 mountinfo、fd、environ、status 实现正确
- [ ] 差分对比算法识别 overlayfs 挂载劫持实现正确
- [ ] 差分对比算法识别目录隐藏实现正确
- [ ] 编译为独立 SO 插件，与微内核接口兼容

## 八、Plugin L5 侧信道时延分析插件检查

- [ ] 锁 CPU 频率、绑定固定核心消除调度误差实现正确
- [ ] `truncate` 系统调用纳秒级时间差采样实现正确（10000 次 0xffff vs 10000 次 0x10000）
- [ ] `openat/readlinkat` 多轮基线时延比对实现正确（识别 syscall Hook）
- [ ] SELinux 上下文多阶段采样跳变检测实现正确（Shamiko 识别）
- [ ] 时间差阈值判断实现正确（500 纳秒基准）
- [ ] 编译为独立 SO 插件，与微内核接口兼容

## 九、Plugin L6 内核完整性探针插件检查

- [ ] 裸读取 `/proc/kallsyms` 解析系统调用表基址实现正确
- [ ] 内核 syscall 函数段哈希校验实现正确（识别内核 Hook）
- [ ] `/proc/modules/sys/modules` 非法内核模块扫描实现正确
- [ ] `kallsyms` 权限隐藏检测实现正确
- [ ] `kallsyms` 内容擦除检测实现正确
- [ ] 编译为独立 SO 插件，与微内核接口兼容

## 十、Plugin L7 TEE TrustZone 硬件可信旁路插件检查

- [ ] 绕过 KeyStore 系统 API，裸读写 `/dev/tee`/`/dev/qseecom` 实现正确
- [ ] TEE 分区镜像完整性校验实现正确
- [ ] 硬件设备基线指纹比对实现正确（识别底层篡改）
- [ ] 编译为独立 SO 插件，与微内核接口兼容

## 十一、规则引擎独立模块检查

- [ ] 加密二进制规则库格式设计正确（`fingerprint.bin.enc`，AES-256 CBC 加密）
- [ ] 规则类型定义完整（内存特征串、内核函数哈希、挂载特征、su 路径、syscall 时延阈值）
- [ ] `rule_parser.c` 规则库加载与解密实现正确
- [ ] 启动时校验 assets 指纹库哈希实现正确
- [ ] 动态热更逻辑实现正确（更新无需重编原生代码）
- [ ] 初始规则库文件创建正确（Magisk/KSU/APatch/LSPosed 特征）
- [ ] 单元测试验证规则引擎功能通过

## 十二、守护进程核心框架检查

- [ ] `daemon_main.c` 独立常驻进程入口实现正确（fork 独立进程，非 SO）
- [ ] 守护进程生命周期管理实现正确（开机跟随 App 启动，最高优先级）
- [ ] 与沙箱进程的双向加密管道通信实现正确
- [ ] 与 UI 进程的加密 Socket 通信实现正确
- [ ] 单元测试验证守护进程启动与通信通过

## 十三、跨域内存可信校验模块检查

- [ ] 周期性扫描 UI 进程、沙箱检测进程全部代码段实现正确（/proc/[pid]/mem）
- [ ] 预存 libdetect 插件、主沙箱程序、classes.dex 多副本哈希实现正确
- [ ] 内存段 patch/注入/修改检测实现正确
- [ ] 高危签名告警生成实现正确
- [ ] 单元测试验证内存校验功能通过

## 十四、反调试 & 进程监控模块检查

- [ ] `ptrace` 附着检测实现正确（PTRACE_TRACEME）
- [ ] 调试端口扫描实现正确（23946、8700 等）
- [ ] tracerpid 校验实现正确（/proc/self/status）
- [ ] 监控沙箱进程是否被 Zygisk 注入、挂载劫持实现正确
- [ ] 拦截外部进程对检测沙箱的读写、ptrace 调试实现正确
- [ ] 检测 Shamiko 全局上下文篡改行为实现正确
- [ ] 单元测试验证反调试功能通过

## 十五、全局验签中心检查

- [ ] 沙箱每层检测结果接收与 SHA3-512 哈希计算实现正确
- [ ] 守护进程使用内置设备唯一密钥对结果签名实现正确
- [ ] 签名验证逻辑实现正确（UI 层调用）
- [ ] 无签名或签名错误判定伪造实现正确
- [ ] 检测到篡改直接标记全局 `CRITICAL_ROOT_TAMPER` 实现正确
- [ ] 单元测试验证验签功能通过

## 十六、内核轻量探针检查

- [ ] 通过裸 syscall 周期性读取内核关键节点实现正确（/proc/kallsyms、/proc/modules）
- [ ] 独立于沙箱检测流程的二次交叉验证实现正确
- [ ] 杜绝单进程被劫持漏报实现正确
- [ ] 单元测试验证内核探针功能通过

## 十七、分段多副本自校验检查

- [ ] 原生程序拆分为 8 段独立哈希实现正确
- [ ] 守护进程存储多副本哈希实现正确
- [ ] 每 3 秒循环比对实现正确
- [ ] 单段篡改立刻告警实现正确
- [ ] 单元测试验证自校验功能通过

## 十八、指令流动态混淆检查

- [ ] 每次加载检测插件时对内部指令流插入随机无运算空指令实现正确
- [ ] 运行时动态变形实现正确
- [ ] 静态逆向失效验证通过
- [ ] 单元测试验证混淆功能通过

## 十九、反 Hook 底层拦截检查

- [ ] 裸 syscall 层内置检测实现正确
- [ ] 识别 libc、libart、libbinder 内联 Hook 实现正确
- [ ] 直接标记环境风险实现正确
- [ ] 单元测试验证反 Hook 功能通过

## 二十、随机内存布局检查

- [ ] 插件加载基址随机映射实现正确（mmap MAP_FIXED_NOREPLACE + 随机地址）
- [ ] 缓冲区地址每次检测重新分配实现正确
- [ ] 固定内存特征逆向无效验证通过
- [ ] 单元测试验证随机布局功能通过

## 二十一、域 A Kotlin 层架构检查

- [ ] `ui/compose` 液态玻璃 UI 框架创建正确（Jetpack Compose）
- [ ] 分级检测面板实现正确（快速/标准/深度）
- [ ] 风险报告可视化实现正确（分层展示、威胁等级颜色标识）
- [ ] 设备信息展示实现正确
- [ ] 确保无任何 NDK JNI 接口、文件操作、进程读取 API
- [ ] UI 层完全独立，零检测逻辑

## 二十二、可信任务调度与 IPC 客户端检查

- [ ] `TrustedViewModel.kt` 可信任务调度 VM 实现正确
- [ ] `SecureSocketClient.kt` 本地 Unix Socket 加密客户端实现正确
- [ ] AES-GCM 签名实现正确
- [ ] 时间戳防重放实现正确
- [ ] 消息序列号防篡改实现正确
- [ ] `TrustModel.kt` 全局统一可信数据模型实现正确
  - ScanTask：下发任务（检测等级、启用插件白名单、随机盐值）
  - TrustedLayerResult：带哈希签名的单层检测输出
  - GlobalSecureReport：全量验签后的完整风险报告
  - SecurityAlert：进程篡改、注入、调试器高危告警
- [ ] UI 收到报告必须携带守护进程签发的哈希签名校验实现正确
- [ ] 无签名直接判定报告伪造实现正确
- [ ] 单元测试验证 IPC 客户端功能通过

## 二十三、检测报告与修复建议系统检查

- [ ] 检测报告数据结构设计正确（GlobalSecureReport）
- [ ] 分层检测结果汇总实现正确
- [ ] 威胁等级评估算法实现正确
- [ ] 报告 UI 界面设计正确（分层展示、威胁等级颜色标识）
- [ ] 检测报告导出功能实现（PDF/JSON）
- [ ] 问题-修复建议映射数据库建立
- [ ] 针对每个检测项的具体修复建议生成实现正确
- [ ] UI 集成修复建议展示成功
- [ ] 单元测试验证报告生成通过

## 二十四、CMake 构建配置检查

- [ ] CMakeLists.txt 编译分离配置正确：7 层插件独立 SO
- [ ] CMakeLists.txt 编译分离配置正确：沙箱主程序
- [ ] CMakeLists.txt 编译分离配置正确：守护独立二进制
- [ ] CMakeLists.txt 编译分离配置正确：底层汇编 syscall 库
- [ ] arm64-v8a 汇编编译器支持配置正确
- [ ] 插件 SO 独立编译与链接配置正确
- [ ] 守护进程独立二进制编译配置正确（非 SO，fork 独立进程）
- [ ] 所有模块编译成功

## 二十五、AndroidManifest 与进程配置检查

- [ ] AndroidManifest.xml 多进程声明配置正确
- [ ] 沙箱检测进程（:sandbox）配置正确
- [ ] 可信守护进程（:daemon）配置正确
- [ ] 进程权限与隔离策略配置正确
- [ ] 多进程启动验证通过

## 二十六、assets 加密规则库打包检查

- [ ] `assets/secure_fingerprint/` 目录创建正确
- [ ] 初始加密规则库 `fingerprint.bin.enc` 打包正确
- [ ] Gradle 打包 assets 配置正确
- [ ] assets 正确打包到 APK 验证通过

## 二十七、性能优化检查

- [ ] 检测算法时间复杂度优化完成
- [ ] 内存扫描性能优化完成
- [ ] 检测结果缓存与增量检测功能实现
- [ ] 性能测试与调优完成（各模式时间达标：快速 <500ms、标准 <3s、深度 <10s）
- [ ] 内存泄漏检测与修复完成

## 二十八、全面测试与 Bug 修复检查

- [ ] 多种 Root 环境组合下测试通过（Magisk+Shamiko、KernelSU+LSPosed 等）
- [ ] Android 10-15 各版本上测试通过
- [ ] 不同设备型号上测试通过（华为、小米、OPPO、vivo、三星等）
- [ ] 发现的所有 Bug 修复完成
- [ ] 回归测试验证修复通过

## 二十九、发布准备检查

- [ ] 应用图标和启动页设计完成
- [ ] 应用描述和使用说明编写完成
- [ ] 签名 APK 生成成功
- [ ] 发布材料准备完成（截图、功能介绍）
- [ ] 第一个公开版本发布成功

## 三十、核心架构优势验证

- [ ] 三进程物理隔离验证：彻底解决单进程全局 Hook 劫持
- [ ] 插件微内核七层检测验证：单一层被篡改不影响其他层校验
- [ ] 密码学可信闭环验证：所有检测结果带守护进程签名，无法内存伪造
- [ ] 规则引擎热更新验证：离线热更新特征库，无需重编 APK
- [ ] 独立 TEE 旁路检测验证：绕过所有上层系统 API 劫持
- [ ] 裸汇编系统调用底层验证：完全规避 libc/ART 全局 Hook
- [ ] 独立可信守护信任根验证：持续监控全部进程内存完整性
- [ ] 动态随机调度验证：消除固定执行时序、内存特征
- [ ] 无 JNI 耦合依赖验证：UI 与原生代码完全通过加密 Socket 隔离，JNI 攻击面归零

## 三十一、扩展兼容性验证

- [ ] 新增 Root 隐藏工具验证：仅需更新 assets 加密指纹库或新增单层插件，上层 UI/守护进程零改动
- [ ] 新增硬件安全检测验证：直接扩展 L7 TEE 插件，不改动微内核调度
- [ ] 新增检测算法验证：独立新增插件，分层隔离
- [ ] 迁移底层实现验证：仅替换域 D 底层抽象层，上层全部兼容

## 三十二、与现有工具对比验证

- [ ] 对 Magisk+Shamiko 组合的检测率 >95%（现有工具 <30%）
- [ ] 对 KernelSU 内核级隐藏的检测率 >90%
- [ ] 对 APatch 的检测率 >95%（现有工具几乎无法检测）
- [ ] 对 LSPosed 的检测率 >90%
- [ ] 综合检测性能优于或等于现有工具
